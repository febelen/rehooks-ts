---
title: useLocalStorage
description: A hook that manages a state variable in sync with local storage, allowing data persistence across browser sessions.
---

# useLocalStorage

A nifty little hook that keeps your state variable in sync with the local storage, ensuring your data sticks around even when the browser session ends.

## Usage

```ts
import { useLocalStorage } from "@febelen/rehooks-ts";

const [name, setName] = useLocalStorage("name", "John Doe");
```

## Parameters

| Name           | Type     | Description                               |
| -------------- | -------- | ----------------------------------------- |
| `key`          | `string` | The key for the local storage item.       |
| `initialValue` | `T`      | The initial value for the state variable. |

## Returns

| Name          | Type                 | Description                                                    |
| ------------- | -------------------- | -------------------------------------------------------------- |
| `storedValue` | `T`                  | The current value synchronized with local storage.             |
| `setValue`    | `(value: T) => void` | A function to update the state and sync it with local storage. |

## Example

```tsx
import React from "react";
import { useLocalStorage } from "@febelen/rehooks-ts";

export default function LocalStorageDemo() {
  const [name, setName] = useLocalStorage("name", "John Doe");

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setName(event.target.value);
  };

  return (
    <div>
      <h2>Local Storage Demo</h2>
      <input
        type="text"
        value={name}
        onChange={handleChange}
        placeholder="Enter your name"
      />
      <p>Your name is: {name}</p>
    </div>
  );
}
```

This hook is a real time-saver, keeping your state in sync with the browser's local storage. No more worrying about losing user data when they refresh or close the page - it's all saved and ready to go for the next visit!
